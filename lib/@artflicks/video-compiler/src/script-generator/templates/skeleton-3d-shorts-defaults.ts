/**
 * Default skeleton reference URLs for the Skeleton 3D Shorts template.
 * Kept in a separate file so the app can import it without pulling in the
 * template module graph (avoids circular dependency with templates/index).
 */
export const DEFAULT_SKELETON_REFERENCES = [
  'https://image.artflicks.app/custom_characters/skeleton-3d-real.jpg',
];
