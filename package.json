{
  "name": "cloudflare-workers-create-story",
  "version": "1.0.0",
  "description": "Cloudflare Workers service for creating short stories with AI-generated images and audio",
  "main": "src/index.ts",
  "workspaces": [
    "lib/@artflicks/*"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "dev:staging": "wrangler dev --env staging",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy --env=\"\"",
    "log": "wrangler tail --format pretty",
    "log:capture": "mkdir -p logs && wrangler tail --format json 2>&1 | tee logs/worker.log",
    "log:capture:stg": "mkdir -p logs && wrangler tail --env staging --format json 2>&1 | tee logs/staging.log",
    "log:capture:prod": "mkdir -p logs && wrangler tail --env production --format json 2>&1 | tee logs/production.log",
    "deploy:stg": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "monitor:stg": "npx wrangler tail --env staging --format pretty",
    "monitor:prod": "npx wrangler tail --env production --format pretty",
    "type-check": "npx tsc --noEmit",
    "build:credit-tracker": "cd lib/@artflicks/credit-tracker && npm run build",
    "build:script-generator": "cd lib/@artflicks/script-generator-templates && npm run build",
    "build:model-provider": "cd lib/@artflicks/model-provider && npm run build",
    "build:all": "npm run build:credit-tracker && npm run build:script-generator && npm run build:model-provider",
    "unused": "knip"
  },
  "keywords": [
    "cloudflare-workers",
    "ai",
    "story-generation"
  ],
  "author": "Rakshith Raj S",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@types/node": "^22.0.0",
    "knip": "^5.78.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.1",
    "@artflicks/model-provider": "file:./lib/@artflicks/model-provider",
    "@supabase/supabase-js": "^2.39.3",
    "ai": "^6.0.3",
    "ai-gateway-provider": "^3.1.1",
    "replicate": "^1.4.0",
    "uuid": "^13.0.0",
    "zod": "^4.2.1"
  }
}
